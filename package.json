{
  "name": "ma-finance-perso",
  "version": "0.0.0",
  "private": true,
  "description": "Ma Finance Perso — gestion financière personnelle 100% mobile (Expo + NestJS/Neon), remplace les 5 classeurs Excel « Smart Finance Pro »",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "mobile": "pnpm --filter mobile start",
    "test": "pnpm --filter @mfp/core test",
    "api": "pnpm --filter api start:dev",
    "typecheck": "pnpm --filter @mfp/core exec tsc --noEmit && pnpm --filter mobile exec tsc --noEmit && pnpm --filter api exec tsc --noEmit",
    "verify:api": "pnpm --filter api verify",
    "db:generate": "pnpm --filter api exec prisma generate",
    "db:migrate": "pnpm --filter api exec prisma migrate deploy",
    "db:studio": "pnpm --filter api exec prisma studio"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "argon2",
      "esbuild",
      "prisma"
    ]
  }
}
